@model CMSSolutions.Websites.Models.DataViewerModel 
@if (Model.IsShow)
{
    <div class="list_episodes-show" style="display: block;">  
        <p class="p_text_film">@Model.Title</p>   
        <div id="show-data" class="content content_mCustomScrollbar_le content_mCustomScrollbar list-film-scroll">@Html.Raw(Model.Data)</div>   
        <br class="clear_both">
        @*<div id="pagination" style="margin-right: 10px;">
            <input id="totalPage" name="totalPage" value="@Model.TotalPage" type="hidden" />
            <input id="pageIndex" name="pageIndex" value="@Model.PageIndex" type="hidden" /> 
            <input id="pageSize" name="pageSize" value="@Model.PageSize" type="hidden" /> 
            <input id="siteId" name="siteId" value="@Model.SiteId" type="hidden" />   
            <input id="filmId" name="filmId" value="@Model.FilmDetails.Id" type="hidden" />    
            <input id="episodeId" name="episodeId" value="@Model.EpisodeId" type="hidden" />
            <div id="show-content" class="content"></div>    
            <div class="clearfix"></div> 
        </div>   *@
        <div class="clear_both"></div>
    </div>

    <script type="text/javascript">
        $(document).ready(function () {
            $("#show-data").mCustomScrollbar({
                theme: "dark-thick",
                scrollInertia: 900,
                scrollEasing: "easeOutQuint",
                mouseWheel: true,
                mouseWheelPixels: 500
            });
            //getData();
        });

        @*function getData() {
            var currentpage = parseInt($("#pageIndex").val());
            var data = $("#pagination").find("input").serialize();
            $.ajax({
                url: '@Url.Action("EpisodesPager", "HomeView")',
                data: data,
                type: 'POST',
                dataType: 'json',
                success: function (result) {
                    $("#show-data").empty();
                    $("#show-data").html(result.Data);
                    $("#totalPage").val(result.TotalPage);
                    $('#show-content').bootpag({
                        total: parseInt($("#totalPage").val()),
                        maxVisible: 10,
                        leaps: true,
                        firstLastUse: true,
                        page: currentpage,
                        first: '←',
                        last: '→'
                    }).on("page", function (event, num) {
                        currentpage = parseInt($("#pageIndex").val());
                        if (num != currentpage) {
                            $("#pageIndex").val(num);
                            getData();
                        }
                    });
                    $('#' + "@Model.SliderName").remove();
                }
            });
        }*@
    </script>
}